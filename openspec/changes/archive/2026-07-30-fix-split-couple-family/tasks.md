## 1. ドメイン: `linkParent` の帰属先を「親の婚姻」で判定する(D1)

- [x] 1.1 `linkParent` の規則2を、親が配偶者として属する家族の総数ではなく、親の婚姻(配偶者2件の家族)の件数で判定するよう変える。婚姻が1件に定まらない場合は従来の「親が属する家族がちょうど1件ならそこへ」をフォールバックとして残す(完了条件: 親が婚姻1件+空き殻1件を持つ状態で `linkParent` すると、配偶者不在の家族が新設されず婚姻の家族の子になる単体テストが通る)
- [x] 1.2 親がもう一方の親が不明なひとり親の家族も持つ場合に、婚姻の家族が帰属先になることを固定する(完了条件: ひとり親の家族の子が変化せず、新しい子は婚姻の家族へ加わる単体テストが通る)
- [x] 1.3 親が複数の婚姻を持つ場合に推測しない従来の挙動が変わっていないことを確認する(完了条件: 既存テスト「親が複数の家族を持つ場合は推測せず、その親だけの家族を新設する」が変更なしで通り続ける)
- [x] 1.4 `linkParent` のドキュメントコメントを、婚姻で数える理由(空き殻1件で規則が働かなくなっていた経緯)まで含めて更新する(完了条件: 判定順の4段が実装と一致して読める)

## 2. ドメイン: 同じ夫婦の家族の統合(D2, D3)

- [x] 2.1 `src/domain/commands.ts` に、配偶者の組み合わせが2人とも一致する別の家族を探す `findCoupleFamily` を追加する(完了条件: 配偶者2件の家族のみを対象とし、指定した家族自身を除外することを検証する単体テストが通る)
- [x] 2.2 家族へ2人目の配偶者を加える処理を `attachSpouse(doc, family, personId)` に集約する。同じ夫婦の家族が既にある場合は、子と婚姻・離婚イベントを既存の家族へ移して1件へ統合し、残った家族のIDを返す(完了条件: 分裂した2家族が1件へ統合され、双方の子がその家族に揃う単体テストが通る)
- [x] 2.3 統合時、同一人物への子リンクを重複させず、種別・場所・日付がすべて一致するイベントを2件へ増やさない(完了条件: 同じ婚姻日が両家族に記録された状態で統合しても婚姻イベントが1件のままで、片方だけの離婚イベントは残る単体テストが通る)
- [x] 2.4 統合時、残す側の関係種別が「不明」で統合元に判明した種別があれば、判明している種別を残す(完了条件: 種別の引き継ぎを検証する単体テストが通る)
- [x] 2.5 `addSpouseLink` を `attachSpouse` 経由に置き換える(完了条件: 同じ夫婦の家族がない場合は従来どおり配偶者参照が2件になり、ある場合は統合される単体テストが両方通る)
- [x] 2.6 `linkParent` の規則1(子のひとり親家族へ2人目の配偶者を加える経路)を `attachSpouse` 経由に置き換え、統合後に残った家族のIDを返す(完了条件: ひとり親家族へもう一方の親をリンクすると既存の夫婦の家族へ統合され、返り値が残った家族のIDになる単体テストが通る)

## 3. UI: 既存の導線が修復導線として働くことの確認(D4)

- [x] 3.1 `FamilyEventEditor` の「配偶者に既存の人物を設定」で既存の配偶者を選ぶと、2つの枠が1つにまとまり双方の子が揃うことを確認する(完了条件: 統合後に「(配偶者未登録)」の枠が消え、両方の子が同じ家族に属するコンポーネントテストが通る)
- [x] 3.2 統合直後のundoで、2つの家族と子の帰属が復元されることを確認する(完了条件: undo後に元の2家族と各々の子が戻るコンポーネントテストが通る)
- [x] 3.3 `PersonPanel` の「親を追加 → 既存の人物から選ぶ」でもう一方の親を選ぶと、夫婦の子として記録し直されることを確認する(完了条件: 統合後に対象の人物が夫婦の家族の子になり、その夫婦の家族が1件だけになるコンポーネントテストが通る)
- [x] 3.4 親に空き殻が残っていても「親を追加 → 既存の人物から選ぶ」で夫婦の家族へ入ることを確認する(完了条件: 配偶者不在の家族が新設されないコンポーネントテストが通る)
- [x] 3.5 既存の配偶者紐づけ・親リンクのコンポーネントテストが変更なしで通り続けることを確認する(完了条件: `PersonPanel.test.tsx` と `FamilyEventEditor.test.tsx` の既存テストが全て通る)

## 4. 報告データによる検証

- [x] 4.1 報告データと同じ形(婚姻+空き殻を持つ人物)へ「親を追加」で子を3人繋いだとき、全員が夫婦の家族の子になることを回帰テストとして固定する(完了条件: 子を持つ家族が1件だけになり、描画データ上の各子の親が2人になるテストが通る)
- [x] 4.2 既に分裂して保存された状態(徳雄のみの家族に 榮、徳雄・ぎんの家族に 兎一)から、「親を追加」で修復できることを回帰テストとして固定する(完了条件: 修復前は描画上の親が1人、修復後は2人になり、徳雄が配偶者として属する家族が1件になるテストが通る)
- [x] 4.3 報告されたJSONを実際のインポート経路(`importFamilyTreeJson`)で読み込み、修復手順を通しで確認する(完了条件: 榮 に ぎん を、紀佳・洋公・英子 に 榮 を「親を追加」で紐づけると、描画データ上いずれも親が2人になる。実データを含むため検証は使い捨てで行い、リポジトリへは同じ構造の回帰テストのみを残す)
- [x] 4.4 読み込み時に自動修復が行われないことを確認する(完了条件: インポート直後の家族数がインポート前のJSONと一致する)

## 5. 全体確認

- [x] 5.1 `npm run lint` / 型チェック / 全テストが通ることを確認する(完了条件: いずれもエラーなし)
- [x] 5.2 保存形式・GEDCOM/JSONの入出力に影響がないことを確認する(完了条件: `SCHEMA_VERSION` に変更がなく、入出力のラウンドトリップテストが通る)
