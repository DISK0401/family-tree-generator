## 1. ドメイン: 削除計画の単一化(D1, D2)

- [x] 1.1 `src/domain/commands.ts` に `isVacantFamily(family)`(配偶者0件、または配偶者1件かつ子0件で `true`)を追加する(完了条件: 配偶者0/1/2件 × 子0/1件の6通りに対する期待値を検証する単体テストが通る)
- [x] 1.2 `src/domain/commands.ts` に `planFamilyRemoval(doc, personId)` を追加し、削除後の `families` と `removedFamilyIds` を同時に返す。`isVacantFamily` の判定は当該削除で内容が変化した家族にのみ適用し、変化しなかった家族はそのまま残す(完了条件: 無関係な配偶者1件・子0件の家族が、別人物の削除で巻き添え削除されないことを検証する単体テストが通る)
- [x] 1.3 `removePerson` を `planFamilyRemoval` の戻り値を使う実装に置き換える(完了条件: 子のいない夫婦の片方を削除すると家族ごと消え、子のいる夫婦の片方を削除するとひとり親家族として残る単体テストが両方通る。既存テスト `commands.test.ts` の「配偶者関係と子の帰属がある人物を削除すると影響件数どおりに整理される」が変更なしで通り続ける)
- [x] 1.4 `computeRemovalImpact` の `removedFamilyCount` を `planFamilyRemoval` の `removedFamilyIds.length` から算出するよう置き換え、独自の判定式を削除する(完了条件: 任意のドキュメントと人物に対して `computeRemovalImpact` の件数と `removePerson` 前後の家族数の差が一致することを検証する単体テストが通る)
- [x] 1.5 `RemovalImpact` に、削除される家族が持つ婚姻・離婚イベントの合計件数を表すフィールドを追加する(完了条件: 婚姻日を持つ子なし夫婦の片方を削除対象にしたときイベント件数1が返る単体テストが通る)

## 2. ドメイン: 既存人物の配偶者紐づけ(D5)

- [x] 2.1 `src/domain/commands.ts` に `addSpouseLink(doc, familyId, personId)` を追加する。家族・人物が存在しない場合は `throw`、既に配偶者の場合は `doc` をそのまま返す(完了条件: 配偶者1件の家族へ既存人物を追加すると `spouseIds` が2件になり、同じ人物を再度追加してもドキュメントが変化しない単体テストが通る)
- [x] 2.2 `addSpouseLink` に、配偶者が既に2件の家族と、当該家族の子である人物を拒否するガードを追加する(完了条件: 両ケースで `throw` することを検証する単体テストが通る)
- [x] 2.3 `addSpouse` の既存挙動(常に新しい家族を作る)が変わっていないことをテストで固定する(完了条件: 配偶者1件・子ありの家族を持つ人物へ配偶者を追加しても既存家族へ合流せず新しい家族が作られる単体テストが通る)

## 3. UI: 人物削除の確認強化(D8)

- [x] 3.1 `src/components/DeletePersonControl.tsx` の確認文に、家族ごと削除される場合に婚姻・離婚の記録も失われる旨を追加する(1.5 のイベント件数を使う)(完了条件: 婚姻日を持つ子なし夫婦の片方を削除しようとしたとき、確認ダイアログにイベントが失われる旨の文言が表示されるコンポーネントテストが通る)
- [x] 3.2 関係がまったくない人物、および家族が削除されない人物では、追加した文言が表示されないことを確認する(完了条件: 既存の「他の人物との関係はありません。」表示が維持され、不要な警告が出ないコンポーネントテストが通る)

## 4. UI: 婚姻単位の削除(D4, D7)

- [x] 4.1 `src/components/FamilyEventEditor.tsx` の各家族の枠に「この婚姻を削除」ボタンを追加する(完了条件: 選択中人物が配偶者として属する家族の数だけボタンが描画されるコンポーネントテストが通る)
- [x] 4.2 削除ボタンに確認ダイアログを実装し(`confirm-dialog.css` と `DeletePersonControl` のパターンを再利用)、当該家族の婚姻・離婚イベント件数と子の帰属件数、および人物自体は削除されない旨を提示する(完了条件: 婚姻日あり・子2人の家族で、件数を含む確認文が表示されるコンポーネントテストが通る)
- [x] 4.3 確認の承認時に `removeFamily` を `apply` 経由で実行する(完了条件: 承認で当該家族が消え、キャンセルではドキュメントが変化しないコンポーネントテストが両方通る)
- [x] 4.4 `src/components/FamilyEventEditor.css` に削除ボタンのスタイルを追加する(完了条件: 婚姻日・離婚日の入力欄と視覚的に競合せず、危険操作として区別できることを実機(`npm run dev`)で確認する)
- [x] 4.5 家族削除がundoで復元できることを確認する(完了条件: 削除直後のundoで家族・イベント・子の帰属が復元されるテストが通る)

## 5. UI: 配偶者が未登録の家族への紐づけ(D6)

- [x] 5.1 `src/components/FamilyEventEditor.tsx` に、他に配偶者が登録されていない家族の枠にのみ配偶者選択の `<select>` を描画する(完了条件: 配偶者登録済みの家族の枠には選択欄が出ず、未登録の枠にのみ出るコンポーネントテストが通る)
- [x] 5.2 候補一覧から、選択中の人物自身・当該家族の既存配偶者・当該家族の子を除外する(完了条件: 子が帰属する配偶者未登録の家族で、本人と子が候補に含まれないコンポーネントテストが通る)
- [x] 5.3 `<select>` の変更で即座に `addSpouseLink` を `apply` する(確定操作を設けない)(完了条件: 候補を選ぶと確定操作なしで枠の見出しが選んだ人物の氏名に変わるコンポーネントテストが通る)
- [x] 5.4 紐づけ後に子の親子関係が図へ反映されることを確認する(完了条件: 配偶者1件・子1人の家族へ配偶者を追加すると、描画データ上その子の親が2人になることを `to-family-chart-data` 経由で検証するテストが通る)
- [x] 5.5 紐づけがundoで取り消せることを確認する(完了条件: 紐づけ直後のundoで配偶者未登録の状態に戻るテストが通る)
- [x] 5.6 `src/components/FamilyEventEditor.css` に選択欄のスタイルを追加し、枠の見出しとの並びを整える(完了条件: 実機(`npm run dev`)で見出し・選択欄・日付欄の視線の流れが破綻していないことを確認する)

## 6. 実データによる検証

- [x] 6.1 報告された家系図データ(富岡兎一を含むJSON)をインポートし、読み込み時点では家族が自動削除されないことを確認する(完了条件: インポート直後の家族数がインポート前のJSONと一致する)
- [x] 6.2 富岡兎一の「(配偶者未登録)」の枠を4群の削除導線で消せることを確認する(完了条件: 削除後に兎一の婚姻・離婚欄が富岡榮との1件のみになる)
- [x] 6.3 富岡英子の婚姻日を持つ空き殻について、確認ダイアログに婚姻イベントが失われる旨が表示されることを確認する(完了条件: 削除前に婚姻日「昭和47年11月7日」を持つ旨が読み取れる確認文が表示される)
- [x] 6.4 富岡徳雄の分裂した家族について、5群の紐づけで富岡ぎんを配偶者に設定し、富岡榮の母として図に描画されることを確認する(完了条件: 紐づけ後に榮の親が徳雄・ぎんの2人として描画される)
- [x] 6.5 6.4 の後に残る重複家族を4群の削除導線で消せることを確認する(完了条件: 徳雄の婚姻・離婚欄が富岡ぎんとの1件のみになり、榮の親子関係が維持される)
- [x] 6.6 一連の操作の前後でGEDCOM/JSONエクスポートが破綻しないことを確認する(完了条件: エクスポート→インポートのラウンドトリップで人物数・家族数・親子関係が一致する)

## 7. 品質チェックとドキュメント

- [x] 7.1 `npm run lint` / `npm run format:check` / `npm run typecheck` / `npm run test` をすべて通す(完了条件: 4コマンドがエラーなく完了する)
  - `lint` / `typecheck` / `test`(304件)は通る。**`format:check` は本変更の前から `origin/develop` の時点で41ファイルが未整形であり、これを解消するには無関係なファイルの全面再整形が必要になるため本変更のスコープ外とした**(CIの品質ゲート `.github/workflows/quality-gate.yml` は `lint`/`typecheck`/`test` のみを実行しており、`format:check` は含まれない)。本変更で新規追加した `src/components/DeletePersonControl.test.tsx` のみ Prettier で整形済み。既存ファイルへの変更分は周囲の記述に合わせている
- [x] 7.2 `README.md` の提供中機能の記述に、婚姻単位の削除と既存人物の配偶者紐づけを反映する要否を判断し、必要なら更新する(完了条件: 更新した、または更新不要と判断した理由がコミットメッセージに残っている)
- [x] 7.3 design.md の Open Questions を解消済みの内容で更新する(配偶者未登録の家族の枠の見出し文言を最終的にどうしたか)(完了条件: Open Questions に結論が追記されている)

## 8. dev / 本番環境での確認

**本群は利用者の判断により対象外とした(2026-07-25)。** 6群の実データ検証をローカルの実機(`npm run dev` + 報告された家系図JSON)で全項目実施済みであり、環境依存の要素を含まない変更のため、dev / 本番での再確認は省略する。

- [x] 8.1 ~~`develop` マージ後、dev環境で6群の検証手順を実機確認する~~(対象外)
- [x] 8.2 ~~本番マージ後、本番環境で同様の動作を確認する~~(対象外)
