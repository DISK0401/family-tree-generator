# Tasks: family-tree-creation-ui

## 1. スパイク(リスク先行検証)

- [x] 1.1 family-chartを導入し、複数配偶者・養子相当リンク・HTMLカスタムカードでの縦書き表示を最小コードで描画検証する(完了条件: 3点の可否判定をdesign.mdのリスク欄に追記し、続行/レンダラ自作転換を決定)

## 2. デザイントークンと画面骨格

- [x] 2.1 デザイントークン `src/styles/tokens.css` を定義する(墨・楮紙・藍・朱+セマンティック色、ライト/ダーク両テーマ、明朝/ゴシックのフォントスタック、タイプスケール)(完了条件: 両テーマでWCAG AAコントラストを確認)
- [x] 2.2 App骨格(キャンバス領域+サイドパネル枠+ヘッダ)をトークン参照のみで実装する(完了条件: ハードコードされた色・フォント指定がないことをレビューで確認)

## 3. ドメインモデル(family-data-model)

- [x] 3.1 型定義(Person / Family / 子リンク+続柄種別 / FuzzyDate / TreeDocument+schemaVersion)と生成ヘルパを実装する(完了条件: 不明情報許容・ひとり親・イベント時系列リストのユニットテストが通る)
- [x] 3.2 和暦⇄西暦変換(明治〜令和の元号テーブル+双方向変換関数)を実装する(完了条件: 改元境界日・「昭和65年」等の不正日付のテストが通る)
- [x] 3.3 日付文字列パース(和暦/西暦/年のみ/「頃・以前・以後・間」修飾子→FuzzyDate)を実装する(完了条件: spec記載の入力例すべてのパーステストが通る)
- [x] 3.4 ドメインコマンド(人物追加・配偶者追加・子追加・親追加・編集・整合的削除・イベント追加)を純関数として実装する(完了条件: 再婚・復縁・養子・削除時の関係整合のテストが通る)
- [x] 3.5 GEDCOM 7.0マッピング対応表を `docs/gedcom-mapping.md` として作成する(完了条件: モデル全フィールドに対応タグまたは拡張タグ方針が記載されている)

## 4. 状態管理と履歴

- [x] 4.1 Zustandストア(コマンド適用+スナップショット方式undo/redo、履歴上限100)を実装する(完了条件: 50操作以上のundo/redo順序のテストが通る)

## 5. ローカル永続化(local-autosave)

- [x] 5.1 idbによる保存・読込・削除レイヤとschemaVersionガード(旧→マイグレーション、新→読み取り中止)を実装する(完了条件: 新旧バージョンデータのガード動作のテストが通る)
- [x] 5.2 デバウンス自動保存+保存状態表示(「端末内にのみ保存」の説明含む)+初回保存時の `navigator.storage.persist()` 要求を実装する(完了条件: 編集→1秒以内の保存と状態遷移表示を実機確認)
- [x] 5.3 データ全削除フロー(影響提示+確認入力+IndexedDBクリア+空状態復帰)を実装する(完了条件: 削除後にIndexedDBが空でundo不能なことを確認)

## 6. 描画(tree-rendering)

- [x] 6.1 変換アダプタ(TreeDocument→family-chart形式)を実装する(完了条件: 再婚・養子・ひとり親・復縁の射影テストが通る)
- [x] 6.2 キャンバスコンポーネント(family-chart描画・ズーム/パン・人物選択・prefers-reduced-motion対応)を実装する(完了条件: 10人規模の図で追加時の自動整列と選択強調を実機確認)
- [x] 6.3 縦書き明朝の人名カード(氏名縦書き+生没年併記+朱の選択マーカー)を実装する(完了条件: 旧字体を含む氏名が両テーマで意図どおり表示される)
- [x] 6.4 系線の意味づけ(養子=破線等)と凡例を実装する(完了条件: 実子/養子の線が視覚的に区別できることを実機確認)

## 7. 編集UI(tree-editor)

- [x] 7.1 空状態ガイドと最初の人物追加フローを実装する(完了条件: データなしで開くとガイドのみ表示→追加でキャンバス中央に表示)
- [x] 7.2 コンテキストアクション(配偶者を追加・子を追加・親を追加、再婚相手の追加)を実装する(完了条件: spec記載の4シナリオを実機確認)
- [x] 7.3 サイドパネル編集フォーム(名前・ふりがな・性別・生没イベント・メモ、即時反映)を実装する(完了条件: 編集確定でカード表示が更新される)
- [x] 7.4 和暦対応日付入力コンポーネント(双方向即時表示・修飾子・エラー表示)を実装する(完了条件: 和暦/西暦入力の相互表示と「昭和65年」エラーを実機確認)
- [x] 7.5 続柄種別の編集と人物・関係の削除フロー(影響範囲の確認ダイアログ)を実装する(完了条件: 実子→養子で線が変わる/削除→undoで復元を実機確認)

## 8. 統合検証と仕上げ

- [x] 8.1 外部送信ゼロを検証する(全編集操作を通してネットワークタブで自オリジン以外のリクエストが0件)(完了条件: 検証手順と結果をPR/変更内に記録)
- [x] 8.2 specs全シナリオの通し確認を行い、自動テスト化されていないものは手動チェックリストとして結果を記録する(完了条件: 全シナリオがpass)
- [x] 8.3 README.mdを更新する(機能概要・プライバシー方針=端末内保存・開発手順)(完了条件: 現状の機能と乖離がない)
- [x] 8.4 developへマージしdev環境で実機確認する(完了条件: dev URLで空状態→家系図作成→再訪復元まで動作)
