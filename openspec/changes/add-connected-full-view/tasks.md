## 1. レイアウトの型と骨格(D2)

- [ ] 1.1 `src/layout/types.ts` に `PedigreeLayout`(人物の配置・家族の結合点・系線の経路・全体の寸法)の型を定義する。DOM・描画ライブラリに依存しないプレーンなデータとする(完了条件: `src/layout` から `family-chart` / `react` / DOM 型を import していないことを確認する)
- [ ] 1.2 `src/layout/graph.ts` に、`TreeDocument` から「家族を結合点とするグラフ」(人物→所属家族、家族→配偶者/子)を構築する処理を追加する。存在しない人物を指す参照は無視する(完了条件: 欠損した子リンクを含むドキュメントで例外が出ず、残りの関係が構築される単体テストが通る)

## 2. 世代の割り当て(D2-1, D6)

- [ ] 2.1 `src/layout/generations.ts` に反復緩和による世代割り当てを実装する。子は親より下、配偶者は同じ層へ揃える(完了条件: 三世代の家系で祖父母・父母・孫が上から順の層になり、婚入した配偶者が相手と同じ層になる単体テストが通る)
- [ ] 2.2 反復回数に上限を設け、収束しない場合はその時点の割り当てを返す(完了条件: 人物Aが人物Bの親かつBがAの親という循環データで、有限時間に終了し両者を含む結果が返る単体テストが通る)
- [ ] 2.3 親子が2層以上をまたぐ場合に失敗しないことを確認する(完了条件: 自分の親と同じ層の人物と婚姻している構成で、例外が出ず層をまたぐ親子関係が保持される単体テストが通る)

## 3. 層内の順序(D2-2, D5)

- [ ] 3.1 `src/layout/ordering.ts` に、夫婦を1つの並び替え単位として扱う層内の初期順序を実装する(完了条件: 夫婦が常に隣接し、きょうだいが連続して並ぶ単体テストが通る)
- [ ] 3.2 親の結合点の位置を基準にした重心法を上下方向へ往復させ、系線の交差を減らす(完了条件: 交差が発生する構成で、往復前より交差数が減ることを検証する単体テストが通る)
- [ ] 3.3 比較をすべて全順序にし、同値は人物ID・家族IDで決着させる(完了条件: 同一ドキュメントに対して2回レイアウトすると全座標が完全一致する単体テストが通る)

## 4. 座標と系線(D2-3)

- [ ] 4.1 `src/layout/coordinates.ts` に、層のy座標と層内のx座標の割り当てを実装する(完了条件: カードが重ならず、層ごとに一定の間隔で並ぶ単体テストが通る)
- [ ] 4.2 親の結合点を子たちの中央へ寄せる調整を実装する(完了条件: 子3人を持つ夫婦で、結合点のx座標が子の中央になる単体テストが通る)
- [ ] 4.3 系線の経路を求める。婚姻線・親子線を種別として区別し、親子線には続柄種別を親子の組ごとに付与する(完了条件: 実子と養子が混在する家族で、それぞれの系線に正しい続柄が付く単体テストが通る)
- [ ] 4.4 実親・養親の双方を持つ人物に、双方の親家族から系線が引かれることを確認する(完了条件: 人物の位置が1つだけで、2本の親子線がそれぞれ「実子」「養子」を持つ単体テストが通る)
- [ ] 4.5 互いに関係を持たない連結成分を、重ならないように並べて配置する(完了条件: 孤立した人物と切り離された夫婦クラスタを含むドキュメントで、全員が配置に含まれカードが重ならない単体テストが通る)

## 5. レイアウタの結合と検証(D1)

- [ ] 5.1 `src/layout/index.ts` に、1〜4を通した `layoutPedigree(doc)` を公開する(完了条件: 婚姻でのみつながる2つの血族が1つの連結した配置になり、妻とその親を結ぶ系線・妻の親どうしの婚姻線が含まれる単体テストが通る)
- [ ] 5.2 婿養子(同じ家族の子どうしが夫婦)の構成を検証する(完了条件: 双方の位置が1つだけで、両者を結ぶ婚姻線と親家族からの系線が得られる単体テストが通る)
- [ ] 5.3 ひとり親の家族を検証する(完了条件: 配偶者1件の家族で親から子への系線が得られ、失敗しない単体テストが通る)
- [ ] 5.4 実データ規模(30名程度)での計算時間を計測して記録する(完了条件: 計測結果を design の Risks へ追記する)

## 6. カード表現の共有(D3)

- [ ] 6.1 `src/rendering/person-card.ts` に `PersonCardView`(表示設定を適用した結果、カードに何を描くか)の型と導出関数を追加する(完了条件: 表示項目・粒度・和暦/西暦の各設定に対して期待どおりの `PersonCardView` が得られる単体テストが通る)
- [ ] 6.2 `PersonCardView` から カードのHTMLを組み立てる関数を切り出す(完了条件: 同じ `PersonCardView` から生成したHTMLが両方の描画系で一致する単体テストが通る)
- [ ] 6.3 `FamilyTreeCanvas` の `setCardInnerHtmlCreator` を 6.1 / 6.2 を使う形へ置き換える(完了条件: 既存の `to-family-chart-data.test.ts` と表示設定まわりのテストが変更なしで全件通り、実機で折りたたみ表示のカードの見た目が変わらないことを確認する)

## 7. つながった全体表示の描画

- [ ] 7.1 `src/rendering/PedigreeCanvas.tsx` を新規作成し、`layoutPedigree` の結果をSVG(系線)+ `foreignObject`(カード)で描画する(完了条件: 三世代の家系が層ごとに描画され、カードが既存の縦書き表現で表示されるコンポーネントテストが通る)
- [ ] 7.2 養子の系線を破線、婚姻線を二重線で描く(完了条件: 実子・養子が混在する家族で、系線のクラスが続柄どおりに付くコンポーネントテストが通る)
- [ ] 7.3 カードのクリックで人物を選択できるようにする。選択によって表示範囲を変えない(完了条件: カードを選ぶと `onSelectPerson` がその人物IDで呼ばれ、SVGのviewBoxが変化しないコンポーネントテストが通る)
- [ ] 7.4 パン・ズームを実装する(完了条件: 実機でドラッグによる移動と拡大縮小ができ、既存キャンバスと操作感が揃っていることを確認する)
- [ ] 7.5 `src/rendering/PedigreeCanvas.css` を追加し、カード・系線のスタイルを既存のデザイントークンで揃える(完了条件: 実機で折りたたみ表示とカードの見た目が一致していることを確認する)

## 8. 表示モードの3値化(D7)

- [ ] 8.1 `FamilyTreeCanvas` の `showAll`(真偽値)を `'collapsed' | 'full' | 'connected'` の列挙型へ置き換える(完了条件: 既存の折りたたみ表示・全体表示の挙動が変わらないことを検証する既存テストが全件通る)
- [ ] 8.2 モードの切り替えUIを3値に対応させ、現在どの表示かが判別できるようにする(完了条件: 3つの表示へ切り替えられ、現在の表示が視覚的に区別できるコンポーネントテストが通る)
- [ ] 8.3 `connected` のとき family-chart のキャンバスに代えて `PedigreeCanvas` を描画する(完了条件: モードを切り替えると描画系が入れ替わり、切り替えで `TreeDocument` が変化しないテストが通る)
- [ ] 8.4 `connected` では「図に現れていない人物」の一覧が空になることを確認する(完了条件: 関係を持たない人物を含む家系図で、`connected` のときトレイのDOMが存在しないテストが通る)
- [ ] 8.5 モードの文言を実機で確認する(完了条件: 3つの違いが文言から読み取れることを確認する。読み取れない場合は design の Open Questions に沿って名称を見直す)

## 9. 通し検証とドキュメント

- [ ] 9.1 表示設定の変更が両方の描画系へ等しく効くことを通しで検証する(完了条件: 姓の非表示・和暦表示・粒度の変更を行い、折りたたみ表示と `connected` で同じ結果になるテストが通る)
- [ ] 9.2 報告済みの実データ相当(婿養子を含む30名規模)を `connected` で描画し、破綻がないことを実機で確認する(完了条件: 重複カードがなく、両家が1枚につながり、養子の系線が破線で描かれることをスクリーンショットで確認する)
- [ ] 9.3 `npm run lint` / `npm run test` / `npm run build` が通ることを確認する(完了条件: 3コマンドすべてが成功する)
- [ ] 9.4 `README.md` の表示モードの記述を3つに更新する(完了条件: 既存の「全体表示モード」の記述と重複・矛盾がないことを読み合わせて確認する)
