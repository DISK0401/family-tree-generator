## 1. ドメイン: バッチ更新の純関数(D3)

- [ ] 1.1 `src/domain/commands.ts` に `bulkUpsertPersons(doc, updates, additions)` を追加する。複数人物の属性更新と新規追加を1回のドキュメント遷移にまとめ、内部は既存の `updatePerson` / `addPerson` 相当の更新の合成とする。全件 no-op の場合は同一参照を返す(完了条件: 更新2件+追加1件が1遷移で適用され store の undo 1回で全て戻る/全件 no-op で `toBe` 同一参照/存在しない personId の update が throw する単体テストが通る)
- [ ] 1.2 性別文字列の解釈純関数を追加する(「男/女/不明」「M/F/U」「male/female/unknown」を受理、空文字は「変更なし」ではなく `unknown` として扱うかを含めて仕様コメント化。解釈不能はエラー)(完了条件: 受理・拒否の対応表どおりの単体テストが通る)

## 2. TSV クリップボード純関数(D4)

- [ ] 2.1 `src/features/person-table/tsv.ts` に TSV の直列化・パース(Excel 方式のダブルクォート引用: セル内タブ・改行・連続引用符)をライブラリ非依存の純関数として実装する(完了条件: 引用内改行・引用内引用符・末尾空セル・空行の各ケースの往復単体テストが通る)
- [ ] 2.2 Excel / Google スプレッドシートの実出力パターン(改行入りセルのコピー結果等)をテストフィクスチャとして固定する(完了条件: 実アプリからコピーした文字列サンプルをパースして期待どおりの行×列になるテストが通る)

## 3. 列定義とセル解釈(D2・D3)

- [ ] 3.1 `src/features/person-table/columns.ts` に列定義(id・見出し・`Person`→表示文字列の導出・セル文字列→`updatePerson` パッチへの解釈)を宣言的な配列として実装する。対象列: 姓・名・姓(ふりがな)・名(ふりがな)・性別(1.2 を使用)・生年月日・出生地・没年月日・没地・メモ、および読み取り専用の「配偶者」列(displayName の連結)。日付は既存 `parseDateInput` で解釈し `original` を保持、表示は既存 `formatDateForDisplay`(表示設定準拠)を使う(完了条件: 各列の導出・解釈・解釈エラーの単体テストが通り、レンダリング層に依存しないことを import で確認できる)

## 4. 表ビュー(閲覧モード)(D1・D6)

- [ ] 4.1 `src/components/PersonTableView.tsx` に `role="grid"` のセマンティックな表を実装する。全人物を1行1人で表示し、列は 3.1 の定義を使う(完了条件: 属性が対応する列に表示される/配偶者列に配偶者の氏名が並ぶ/表示設定を和暦へ変えると日付列が追従するコンポーネントテストが通る)
- [ ] 4.2 並べ替え(氏名・生年月日。日付未入力は末尾=既存 `compareFuzzyDate` に揃える)と絞り込み(氏名・ふりがなの部分一致)を実装する(完了条件: spec のシナリオ2本に対応するテストが通り、並べ替え・絞り込みでドキュメントが変化しない)
- [ ] 4.3 App に「図 / 表」のビュー切替を追加する。切替はデータを変更せず、パネルに未確定の変更がある場合は既存の離脱確認を通す。表モードでは PersonPanel を表示しない(完了条件: 切替でドキュメント不変/dirty 時に確認が出て「編集を続ける」で切替中止のテストが通る)
- [ ] 4.4 選択の共有を実装する: 行クリックで `selectedPersonId` を更新し、表を開いたとき選択中の行を視認できる位置へスクロールし、図へ戻るとその人物が選択されている(完了条件: spec「表で選んだ人物が図でも選択されている」のテストが通る)

## 5. 編集モード: セル編集(D3・D5)

- [ ] 5.1 閲覧/編集モードの明示トグルを実装する。閲覧モードではセル編集を開始できない(完了条件: 閲覧モードでクリック・キー入力しても編集が始まらないテストが通る)
- [ ] 5.2 セルエディタを実装する: テキスト系は input、性別は select、F2/直接タイピングで編集開始、Enter=確定して下・Tab=確定して右・Esc=取消。確定は `updatePerson` を 1 回 `apply`(値が変わらなければ発行しない)(完了条件: 編集確定でカード/パネル側にも反映され undo 1回で戻る/no-op 確定で履歴が増えないテストが通る)
- [ ] 5.3 IME ガードを実装する: `compositionstart`〜`compositionend` の間は Enter/矢印をセル移動として扱わない(完了条件: composition イベントを合成したテストで、変換確定の Enter がセル確定・移動を起こさない)
- [ ] 5.4 日付セルの解釈エラー表示を実装する: 解釈できない確定は人物データを変更せず、セルにエラーマーカーと理由を表示する(完了条件: 「昭和99年13月40日」で値不変+エラー表示のテストが通る)

## 6. 編集モード: 矩形選択・コピー・ペースト(D4・D5)

- [ ] 6.1 矩形選択を実装する: アンカー+Shift+クリック / Shift+矢印、`aria-selected` の同期(完了条件: 2×2 選択の範囲判定と aria 属性のテストが通る)
- [ ] 6.2 `onCopy` で選択範囲を TSV としてクリップボードへ書き出す(セルの表示文字列、2.1 の引用符処理)(完了条件: 2×2 コピーで TSV 2行2列/改行入りメモが引用符で1セルになるテストが通る)
- [ ] 6.3 `onPaste` を実装する: TSV を起点セルから行×列に展開し、3.1 の解釈を通して **`bulkUpsertPersons` 1回**で適用する。解釈できないセルは適用せず、行×列と理由の結果サマリ+セルのエラーマーカーで明示する(部分適用)。絞り込み中は表示行にのみ適用する(完了条件: 正常2×2 貼り付け/不正日付混在の部分適用+サマリ/undo 1回で貼り付け全体が戻る/絞り込み中に非表示行が変わらないテストが通る)
- [ ] 6.4 選択範囲の Delete(範囲クリア)も `bulkUpsertPersons` 1回で適用する(完了条件: 2×2 クリアが undo 1回で戻るテストが通る)
- [ ] 6.5 編集モード中は並べ替えを固定する(完了条件: 編集モードで列見出しの並べ替え操作が無効になるテストが通る)

## 7. 行追加(=人物の追加)

- [ ] 7.1 表末尾のゴースト行を実装する: いずれかのセルに非空値が確定した時点で `addPerson`(関係なし登録と同等)。全セル空のままでは人物を作らない(完了条件: 姓の入力確定で1人追加され図側の既存挙動(未接続一覧等)に現れる/空のまま離脱で追加されないテストが通る)
- [ ] 7.2 ペースト範囲が既存行数を超える場合、超過行を新規人物として同一バッチ(6.3)に含める(完了条件: 残り1行起点に3行貼りで「更新1+追加2」が undo 1回で戻るテストが通る)

## 8. 既存の編集経路との等価性

- [ ] 8.1 保存が停止する状態(blocked / unavailable / stale)では編集モードを利用不可にし、既存の案内表示に従う(完了条件: stale 状態で編集モードへ入れず、既存の再読み込み導線が表示されるテストが通る)
- [ ] 8.2 表の編集が自動保存に乗ることを確認する(完了条件: セル編集確定→「保存済み」表示、fake-indexeddb 経由でリロード相当の復元ができる統合テストが通る)

## 9. スタイル・アクセシビリティ

- [ ] 9.1 和紙トークン(ライト/ダーク両テーマ)でのスタイルを実装する: 表・固定ヘッダー・選択範囲・エラーマーカー・横スクロール(モバイルは閲覧可能レベル)(完了条件: 全域でトークン変数のみを参照し、ハードコード色がないことを確認する)
- [ ] 9.2 grid パターンの a11y を検証する: role 構造(grid/row/columnheader/gridcell)、roving tabindex、選択状態の読み上げ(完了条件: role とキーボード到達性のコンポーネントテストが通る)

## 10. 性能・E2E・全体検証

- [ ] 10.1 1,000人規模の描画・貼り付け性能テストを追加する(緩い上限。D7 の回帰監視)(完了条件: 生成データ1,000人で表描画と100行貼り付けが上限内に収まるテストが通り、実測値を design の Risks へ追記する)
- [ ] 10.2 E2E を1本追加する: 表を開いて編集モードで TSV を貼り付け、図(または人物数)に反映されることを実ブラウザで確認する。既存の外部送信ゼロ E2E が変更なしで通ることも確認する(完了条件: `npm run test:e2e` 全件パス)
- [ ] 10.3 全体検証: `npm run lint` / `npm run format:check` / `npm run typecheck` / `npm run test` / `npm run build` を各 exit code まで確認する(完了条件: すべて成功)

## 11. ドキュメント

- [ ] 11.1 README の機能一覧(無料版)へ「人物一覧の表形式ビューと Excel ライク編集モード」を追記し、アーキテクチャ節へ `src/features/person-table/` を追加する(完了条件: 記載内容が実装と一致している)
