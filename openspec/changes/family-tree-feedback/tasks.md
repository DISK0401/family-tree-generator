## 1. スパイク: 全体表示モードの実現可能性調査

- [x] 1.1 family-chart(`node_modules/family-chart`)のソースを確認し、`main_id` による祖先/子孫走査を経由せずに任意のノード集合を描画するAPI相当が存在するか調査する(完了条件: 存在有無と、存在しない場合の代替実装方針を design.md の Open Questions に追記した状態でコミットする)
- [x] 1.2 調査結果に基づき、全体表示モードのD5(design.md)の実装方針(「最大連結範囲のmain_idを固定」案でよいか、非連結クラスタの扱い)を確定する(完了条件: 方針が決まり、以降のタスク2群の実装に着手できる)

## 2. 描画アダプタ: 決定的な並び順と家族優先順位(D1, D2)

- [x] 2.1 `src/rendering/to-family-chart-data.ts` の `parentsByChild` 選択ロジックを、`pedigree !== 'biological'` を優先するよう変更する(完了条件: 夏目漱石サンプルで `soseki` の主たる親子線が `f-shiobara`(養親)側になる単体テストが通る)
- [x] 2.2 子の並び順比較関数(生年昇順→不明時は名前順)を純関数として実装し、`FamilyTreeCanvas.tsx` で `chart.setSortChildrenFunction(...)` に渡す(完了条件: 比較関数単体のテストで生年混在・生年不明混在のケースが期待順序になる。family-chart公式フック経由であることをコードレビューで確認する)
- [x] 2.3 配偶者の並び順比較関数(婚姻イベント日付昇順→不明時は登録順)を純関数として実装し、`chart.setSortSpousesFunction(...)` に渡す(完了条件: 比較関数単体のテストで渋沢栄一サンプル相当のケース(`f-chiyo` が `f-kaneko` より先)が通る)
- [x] 2.4 上記2.1〜2.3の比較関数の単体テストを追加する(完了条件: `npm run test` で新規テストがすべて成功する)
- [x] 2.5 `main_id` 追従(`updateMainId`)がある状態でも、クリックのたびに子・配偶者の並び順が変化しないことを実機(`npm run dev`)で確認する(完了条件: 性別未設定の人物を含む家系図で複数回クリックしても並び順が変わらない。半きょうだいが混在する渋沢栄一サンプルでも婚姻単位のグルーピングは許容し、単婚ケースの安定性を重点確認する)

## 3. 描画: 全体表示モードと折りたたみ人数バッジ(D5, D6)

- [x] 3.1 全体表示モードのトグルUI(キャンバス操作エリアまたはヘッダー)を追加する(完了条件: トグルで表示状態が視覚的に切り替わる)
- [x] 3.2 全体表示モード用の `main_id` 決定ロジック(最大連結範囲の根を算出するBFS)を実装する(完了条件: 単一連結の家系図で全人物が表示される単体/結合テストが通る)
- [x] 3.3 全体表示モード有効時に `chart.setShowSiblingsOfMain(true)` を設定し、`chart.updateMainId(...)` の呼び出しをスキップするよう `FamilyTreeCanvas.tsx` を変更する(完了条件: モード有効中にカードを選択しても表示範囲が変わらず、`main_id`本人の兄弟姉妹も表示されることを実機確認する)
- [x] 3.4 折りたたみ表示時の非表示人数を算出するロジック(境界人物ごとのBFS)を実装する(完了条件: 非表示クラスタのサイズが期待値と一致する単体テストが通る)
- [x] 3.5 非表示人数バッジをカードUIに追加し、全体表示モード中は表示しないようにする(完了条件: 折りたたみ表示でバッジが出て、全体表示モードに切り替えると消えることを実機確認する)

## 4. 描画: 性別インジケーターと年齢表示(D7, D8)

- [x] 4.1 人物カードに性別インジケーター(アイコン+色。選択強調の「朱」とは別配色)を追加する(完了条件: 男・女・不明の3状態が色だけでなく形状でも判別できることを実機確認する)
- [x] 4.2 満年齢/没年齢の計算関数を `src/domain/` に実装する(生年が年のみの場合は `undefined` を返す)(完了条件: 存命・故人・年のみ判明の3パターンで期待値を返す単体テストが通る)
- [x] 4.3 人物カードに年齢表示を追加する(完了条件: 4.2の関数の戻り値が `undefined` のとき年齢が表示されないことを実機確認する)

## 5. 表示設定機能(display-settings、D9)

- [x] 5.1 表示設定(生年月日・没年月日の粒度)を保持する `localStorage` ベースの永続化モジュールを実装する(完了条件: 保存→リロード→復元が単体テストで確認できる)
- [x] 5.2 表示設定を変更するUI(設定メニュー内)を追加する(完了条件: 選択した粒度が即座に人物カードへ反映されることを実機確認する)
- [x] 5.3 表示粒度に応じたカード上の日付フォーマット処理を実装する(年のみ/年月まで/年月日まで)(完了条件: 3段階それぞれで正しい表示文字列になる単体テストが通る)
- [x] 5.4 未設定時のデフォルト値(年月日まで)を実装する(完了条件: `localStorage` が空の状態でエディタを開くと従来通り年月日まで表示される)
- [x] 5.5 表示設定の変更が `TreeDocument` の undo/redo 履歴・エクスポート内容に影響しないことを確認する回帰テストを追加する(完了条件: 表示設定変更後にundoしても直前の家系図編集操作のみが取り消される)

## 6. 人物編集フォーム: 未確定変更の確認とEnter確定(D3)

- [x] 6.1 `PersonEditForm` にダーティ状態検知(`onDirtyChange` コールバック)を追加する(完了条件: 入力を変更するとダーティになり、確定するとダーティが解除される単体テストが通る)
- [x] 6.2 `PersonPanel`/`App.tsx` に選択切り替えのガード(`requestChangeSelection`)を実装し、直接 `setSelectedPersonId` を呼ばないよう変更する(完了条件: ダーティ状態で別人物を選択しようとすると選択が即座には切り替わらないことを実機確認する)
- [x] 6.3 未確定変更の確認ダイアログ(続ける/破棄して移動/保存して移動)を既存の `confirm-dialog` パターンで実装する(完了条件: 3つの選択肢がそれぞれ設計通りに動作することを実機確認する)
- [x] 6.4 ダイアログの主要ボタンに `autoFocus` を設定し、Enterキーで選択操作が実行できることを確認する(完了条件: キーボードのみでダイアログを操作できることを実機確認する)
- [x] 6.5 `PersonEditForm` のテキスト入力でEnterキーを押すと標準のフォーム送信仕様により「確定」が実行されることを回帰確認する(完了条件: 既存の`preventDefault`等の妨げがないことをコードレビューと実機確認の両方で確認する)

## 7. 日付入力: 8桁数字形式(D4)

- [x] 7.1 `src/domain/parse-date.ts` に `GREGORIAN_COMPACT_RE`(`^(\d{4})(\d{2})(\d{2})$`)を追加し、`parseCore` から呼び出す(完了条件: `20000101` が `{year:2000, month:1, day:1}` にパースされる単体テストが通る)
- [x] 7.2 `src/domain/parse-date.test.ts` に8桁形式の正常系・不正日付(存在しない日付)のテストケースを追加する(完了条件: `npm run test` で成功する)
- [x] 7.3 ハイフン区切り(`2000-01-01`)が既存実装のまま正しく動作することを回帰確認する(完了条件: 既存テストが継続して成功する。新規実装は不要)

## 8. エクスポートファイル名のタイムスタンプ(D10)

- [x] 8.1 14桁タイムスタンプ(`yyyyMMddHHmmss`、ローカル時刻)を生成するユーティリティ関数を実装する(完了条件: 固定日時を注入した単体テストで期待文字列を返す)
- [x] 8.2 `src/components/ImportExportControl.tsx` のJSON/GEDCOM(7.0・5.5.1)エクスポート箇所のファイル名を `family-tree-{timestamp}.json` 等の形式に変更する(完了条件: 実機でエクスポートし、ファイル名にタイムスタンプが含まれることを確認する)
- [x] 8.3 短時間に複数回エクスポートしてもファイル名が重複しないことを実機確認する(完了条件: 1分以内に2回エクスポートし、2つの異なるファイルが保存される)

## 9. Windows開発環境の改善(D11)

- [ ] 9.1 現在解決されている `rolldown` のバージョンに合わせ、`win32-x64-msvc` / `darwin-arm64` / `darwin-x64` / `linux-x64-gnu` の `@rolldown/binding-*` をルート `package.json` の `optionalDependencies` に追加する(完了条件: `package.json` に該当エントリが追加される)
- [ ] 9.2 `package-lock.json` を再生成する(完了条件: 追加した全プラットフォーム分の `@rolldown/binding-*` が `node_modules/...` エントリとしてロックファイルに書き込まれる)
- [ ] 9.3 再生成後に `npm run lint` / `npm run typecheck` / `npm run test` / `npm run build` を実行し、`optionalDependencies` 追加以外の意図しない依存バージョン変動がないことを `git diff` で確認する(完了条件: 4コマンドすべてが成功し、diffが追加分のみであることを確認する)
- [ ] 9.4 README.mdのセットアップ手順から、Windows向けの手動インストール手順(該当する記載があれば)を削除する(完了条件: README.mdに `npm install` のみで完結する旨が記載されている)

## 10. ブランディング統一(D12)

- [ ] 10.1 `public/favicon.svg` を、既存デザイントークンを用いた「家系図帖」を想起させる意匠へ差し替える(完了条件: ブラウザタブでファビコンを確認し、汎用図形から置き換わっていることを確認する)
- [ ] 10.2 `src/App.css` の `.app-header` と `src/pages/LandingPage.css` の `.landing-header` が共通のデザイントークンを参照するよう統一する(完了条件: 2つのページのヘッダーの高さ・余白を並べて比較し、視覚的に揃っていることを確認する)
- [ ] 10.3 `src/App.tsx` のエディタヘッダー見出しを「家系図」から「家系図帖」に変更する(完了条件: `/app` を開いたときヘッダーに「家系図帖」と表示される)

## 11. ドキュメント更新

- [ ] 11.1 README.mdに、本変更で追加した主要機能(全体表示モード・表示設定・タイムスタンプ付きエクスポート等)の記載を追加・更新する(完了条件: README.mdの記載が実装内容と一致する)

## 12. 最終確認

- [ ] 12.1 `npm run lint` / `npm run typecheck` / `npm run test` / `npm run build` をすべて実行し成功することを確認する(完了条件: 4コマンドすべてが非ゼロ終了しない)
- [ ] 12.2 `openspec validate family-tree-feedback --type change --strict` を実行し、specとの整合を確認する(完了条件: validが返る)
- [ ] 12.3 夏目漱石サンプル・渋沢栄一サンプルをエディタで開き、養子縁組・再婚がそれぞれ視覚的に分かりやすく表示されることを目視確認する(完了条件: バグ報告で指摘された2点が解消されていることを確認する)
