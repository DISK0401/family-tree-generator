## 1. 元号データの整備

- [ ] 1.1 信頼できる典拠(国立公文書館・国立国会図書館等の元号一覧)を確認し、大化(645年)〜明治直前(慶応)までの元号・改元日(グレゴリオ暦換算)一覧を洗い出す。南北朝期(1336-1392年)は北朝系列のみを採用する。完了条件: 元号名・開始日(年月日)の一覧表(作業メモ)ができている
- [ ] 1.2 `src/domain/wareki.ts` の `ERA_TABLE` に、洗い出した元号を末尾(明治より前)へ追加する。既存の型(`Era`)・並び順(新しい元号ほど先頭)の規約を踏襲する。完了条件: `ERA_TABLE` が大化〜令和までの全行を含み、既存の明治〜令和の5行に変更がない
- [ ] 1.3 追加した各元号の `start`/`end` が前後の元号と隙間なく連続していることを目視レビューする。完了条件: 1.4のテストが通る前提のセルフレビュー完了

## 2. データ整合性のテスト

- [ ] 2.1 `src/domain/wareki.test.ts` に、`ERA_TABLE` の元号名が重複していないことを検証するテストを追加する。完了条件: 意図的に重複行を仕込むと失敗するテストであることを確認済み
- [ ] 2.2 `src/domain/wareki.test.ts` に、隣接する元号の `end` と次の元号の `start` が連続している(隙間・重複がない)ことを検証するテストを追加する。完了条件: 境界日をずらすと失敗するテストであることを確認済み
- [ ] 2.3 `src/domain/wareki.test.ts` に、各元号の `start` が `end`(存在する場合)より前であることを検証するテストを追加する。完了条件: `start > end` の行を仕込むと失敗する

## 3. 明治6年以前の日付妥当性チェックの緩和

- [ ] 3.1 `src/domain/calendar-date.ts` の `isValidCalendarDate` の呼び出し元(`wareki.ts`)で、明治6年(1873年)1月1日より前のグレゴリオ暦換算日については日を1〜30日の範囲で妥当とみなす分岐を実装する(design.mdの決定2)。閏月には対応しない(月は1〜12のみ)。完了条件: 既存の明治6年以降の挙動(`isValidCalendarDate`)に変更がない
- [ ] 3.2 `src/domain/wareki.test.ts` に、明治6年より前の日付(例: 安政3年2月30日)がグレゴリオ暦の同名月の日数を超えても受理されること、31日は拒否されることを検証するテストを追加する。完了条件: specsのシナリオ「明治6年より前の日付は暦法非依存の緩い妥当性で受理される」「範囲外の日は拒否される」に対応するテストがある

## 4. 江戸期以前の元号入力の動作確認

- [ ] 4.1 `src/domain/parse-date.test.ts` に、江戸期元号(例: 文久・安政)を含む日付文字列のパースが成功することを検証するテストを追加する。完了条件: `parseDateInput('文久2年5月10日')` が対応する西暦へ変換される
- [ ] 4.2 `src/domain/wareki.test.ts` に、南朝の元号(例: 天授)を入力した場合に「元号「天授」には対応していません」というエラーになることを検証するテストを追加する。完了条件: specsのシナリオ「南朝の元号は非対応」に対応するテストがある
- [ ] 4.3 `src/components/WarekiDateInput.test.tsx` に、江戸期元号での入力→西暦即時表示のシナリオを1件追加する。完了条件: `npx vitest run src/components/WarekiDateInput.test.tsx` が通る

## 5. 周辺機能への影響確認

- [ ] 5.1 `src/lib/gedcom/dateMapping.test.ts` に、明治より前(江戸期)の和暦日付を含むGEDCOMラウンドトリップのテストケースを追加する。完了条件: インポート→エクスポートで日付が変化しない
- [ ] 5.2 `src/lib/json/roundtrip.test.ts` に、同様の江戸期日付を含むJSONラウンドトリップのケースがあるか確認し、なければ追加する。完了条件: ラウンドトリップでデータが変化しない

## 6. ドキュメント更新

- [ ] 6.1 README.mdの和暦入力に関する説明(生年月日/没年月日編集の項)に、対応元号が明治以降に限らず日本の元号全体(大化〜令和、北朝系列)に拡大したことを追記する。完了条件: README.mdの記述が実装と一致する

## 7. 品質チェック

- [ ] 7.1 `npm run lint` / `npm run format:check` / `npm run typecheck` / `npm run test` をすべて実行し、パスすることを確認する。完了条件: 4コマンドすべてが成功する
- [ ] 7.2 `npm run build` が成功することを確認する。完了条件: ビルドエラーがない
