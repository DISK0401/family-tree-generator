# Tasks: add-web-product-intro

## 1. ルーティング基盤

- [x] 1.1 `main.tsx` にパス判定を実装し、`/app` 配下でエディタ(`App`)、それ以外でランディングを表示する(完了条件: `/`・`/app`・`/unknown` の3パスで表示コンポーネントが切り替わることをテストで確認)
- [x] 1.2 `React.lazy` + `Suspense` でランディング/エディタをルート単位にコード分割する(完了条件: `npm run build` の出力でエディタ用チャンク(family-chart含む)とランディング用チャンクが分離されている)
- [x] 1.3 既存エディタが `/app` で従来どおり動作することを確認する(完了条件: 既存のApp系テストがすべて通り、IndexedDB保存データが `/app` で表示される)

## 2. ランディング骨格

- [x] 2.1 `src/pages/LandingPage.tsx` + `LandingPage.css` を作成し、ヘッダー(「アプリを開く」常設リンク)・ヒーロー(キャッチコピー+CTA「無料ではじめる」)・フッターの骨格を実装する(完了条件: `/` でヒーローCTAとヘッダーリンクの2導線から `/app` へ遷移できる)
- [x] 2.2 ヒーロー用の家系図SVGイラスト(トークン色参照・架空の人名)を作成し配置する(完了条件: ライト/ダーク切替でSVGの配色が追従する)
- [x] 2.3 フッターに観賞用・記念用の注意書きを掲載する(完了条件: 事実証明文書ではない旨の文言がフッターに表示される)

## 3. 機能紹介セクション

- [x] 3.1 提供中機能5点(ブラウザ完結・和暦対応・複雑な家族関係の描画・自動保存・GEDCOM/JSON入出力)のカードグリッドをSVG図版付きで実装する(完了条件: 5枚のカードが図版+説明文付きで表示され、和暦・養子/再婚/複数配偶者への言及がある)
- [x] 3.2 Coming Soon機能(戸籍スキャン自動生成・クラウド保存/親族共有・高品質印刷/PDF出力)のカードを「実装予定」バッジ+彩度抑制で実装する(完了条件: 提供中カードと視覚的に区別でき、クリックしても何も起動しない)
- [x] 3.3 戸籍スキャン紹介文を法務ルールに沿って記述する(完了条件: 「ご自身で取得した戸籍」「確認・修正を前提」が読み取れる文言になっている)
- [x] 3.4 プライバシー訴求セクション(データはこの端末のみ・サーバ送信なし)を実装する(完了条件: 送信ゼロの説明セクションが表示される)

## 4. サンプルデータ作成

- [x] 4.1 徳川家康サンプル(正室・側室・主要な子女による複数配偶者パターン、和暦の生没年)を `TreeDocument` 形式で作成する(完了条件: エディタに読み込むと正室・側室と子女が正しく描画される)
- [x] 4.2 夏目漱石サンプル(養子縁組パターン)を作成する(完了条件: 養子関係がエディタで正しく描画される)
- [x] 4.3 渋沢栄一サンプル(死別後の再婚パターン、旧字体表記「澁澤榮一」を含む)を作成する(完了条件: 再婚関係と旧字体人名が正しく表示される)
- [x] 4.4 架空の現代家族サンプル(架空である旨をデータ内の注記に含む)を作成する(完了条件: 架空明記があり、現代的な家族構成が描画される)
- [x] 4.5 各サンプルの系譜内容をWikipedia等の公知情報と突き合わせ、故人のみで構成されていることを確認する(完了条件: 存命人物・皇室が含まれないことのチェックリストを検証記録に残す)

## 5. サンプルギャラリー(ランディング側)

- [x] 5.1 4サンプルの静的SVG図版(トークン色参照)を作成する(完了条件: 各図版で複数配偶者/養子/再婚のパターンが視覚的に読み取れ、テーマに追従する)
- [x] 5.2 タブ/カード切替のギャラリーUIを実装する(完了条件: サンプル選択で図版と説明が切り替わる)
- [x] 5.3 「Wikipedia等の公知情報を基に簡略化したサンプル」注記と架空明記を表示する(完了条件: 全サンプルに注記が表示される)
- [x] 5.4 各サンプルに「このサンプルをエディタで開く」導線(`/app?sample=<id>`)を付ける(完了条件: リンク先URLが正しく、クリックでエディタに遷移する)

## 6. サンプル読み込み(エディタ側)

- [x] 6.1 `/app?sample=<id>` のクエリ検出とサンプルデータの動的import(エディタチャンク側)を実装する(完了条件: 空の状態でサンプルが即時読み込まれ、不正IDでは通常起動する)
- [x] 6.2 既存データ(人物1名以上)がある場合の上書き確認ダイアログを実装する(完了条件: 確認まで読み込まれず、拒否時は既存データが保持されることをテストで確認)
- [x] 6.3 読み込み/拒否後に `history.replaceState` でクエリを除去する(完了条件: リロードしても確認ダイアログ・再読み込みが発生しない)
- [x] 6.4 読み込んだサンプルが自動保存・編集・エクスポートの対象になることを確認する(完了条件: サンプル読み込み後の編集がIndexedDBに保存され、GEDCOM/JSONエクスポートできる)

## 7. メタ情報・OGP

- [x] 7.1 `index.html` に日本語title・meta description・OGP(og:title/og:description/og:image/og:type)・twitter:cardを追加する(完了条件: HTMLソースに各タグが存在し、og:imageが自オリジンURLを指す)
- [x] 7.2 OGP画像(1200×630 PNG)を作成し `public/ogp.png` に配置する(完了条件: 画像が配信され、OGPタグから参照されている)

## 8. 品質確認

- [x] 8.1 ランディングのユニットテスト(表示セクション・導線・Coming Soon区別・法務文言)を作成する(完了条件: `npm run test` が通る)
- [x] 8.2 サンプル読み込みのユニットテスト(空/既存あり/不正ID/クエリ除去)を作成する(完了条件: `npm run test` が通る)
- [x] 8.3 `npm run lint` / `npm run typecheck` / `npm run build` を通す(完了条件: 3コマンドすべてエラーなし)
- [x] 8.4 レスポンシブ(375px)・ライト/ダークテーマ・外部通信ゼロ・ランディングでエディタチャンク未読込をブラウザで確認する(完了条件: 確認結果を検証記録に残す)

## 9. ドキュメント

- [x] 9.1 README.mdを更新する(ページ構成 `/` と `/app`、サンプルギャラリー、OGP画像の運用注意)(完了条件: READMEが新構成を反映している)

## 10. デプロイ検証

- [ ] 10.1 dev環境へデプロイし、`/app` 直アクセス・`/app?sample=<id>` 直アクセス・OGPメタ・noindexヘッダを検証する(完了条件: 検証結果をtasks.mdまたは検証記録に記載)
- [ ] 10.2 本番デプロイ後、`/` ランディング・`/app` エディタ・既存利用者データの継続表示を確認する(完了条件: 本番検証結果を記録)

---

## 検証記録(2026-07-18 実装時)

### 4.5 サンプル人選チェックリスト

| サンプル | 全員故人 | 皇室なし | 実在の存命人物なし | 備考 |
|---|---|---|---|---|
| 徳川家康 | ✓(最晩年の人物: 亀姫 1625年没) | ✓ | ✓ | 系譜は通説の範囲。不確実な生年は qualifier: about で保持 |
| 夏目漱石 | ✓(最晩年の人物: 筆子 平成元年頃没) | ✓ | ✓ | 塩原家への養子と夏目家復籍は公知の経歴 |
| 渋沢栄一 | ✓(最晩年の人物: 武之助 1946年没) | ✓ | ✓ | 千代との死別(明治15年・コレラ)→兼子との再婚(明治16年)は公知 |
| 現代の家族 | -(全員架空) | ✓ | ✓ | タイトル・全人物noteに架空である旨を明記 |

- いずれもWikipedia等で公知の系譜情報の範囲に簡略化し、ランディングの図版・注記に「公知情報を基に簡略化したサンプル」と表示している。

### 10.1 dev環境デプロイ(2026-07-18、部分完了)

- PR #15 を `develop` へマージ(マージコミット `97f7841`)。quality gate は修正2件(jsdom向けDOMMatrixスタブ追加・lintエラー修正)の後に成功。
- deploy.yml run 29639276212 が成功し、dev環境 `https://family-tree-generator-dev.disk0401.workers.dev` へデプロイ完了(Version ID: 285f6dcc-a3b4-4147-85fa-02e9621b4906)。
- デプロイログで以下を確認済み: `index.html`・`ogp.png`・ランディング/エディタ/サンプル各チャンクの計13アセットがアップロードされ、`ENVIRONMENT="dev"` バインディング(noindexヘッダ付与の条件)が有効。
- **未完(要手動確認)**: 実URLへのHTTPアクセス検証は、作業セッションのネットワークポリシー(外部サイトへの接続不可)のため実施できていない。以下をブラウザまたはcurlで確認すること:
  ```bash
  BASE=https://family-tree-generator-dev.disk0401.workers.dev
  curl -sI "$BASE/"                              # 200・X-Robots-Tag: noindex
  curl -s "$BASE/" | grep og:                    # OGPメタ
  curl -sI "$BASE/app"                           # 200(SPAフォールバック)
  curl -sI "$BASE/app?sample=tokugawa-ieyasu"    # 200
  curl -sI "$BASE/ogp.png"                       # 200 image/png
  ```
  ブラウザでは `/` のランディング表示・サンプルギャラリー・「エディタで開く」でのサンプル展開を目視確認する。

### 8.3 / 8.4 品質確認結果

- `npm run lint` / `npm run typecheck` / `npm run test`(193件) / `npm run build` すべて成功。
- ビルドチャンク: LandingPage 13.1KB / App 283.3KB(family-chart含む)/ サンプルデータは各2〜4KBの個別チャンクに分離。
- `vite preview` + headless Chromium での実ブラウザ確認:
  - `/`(1280px・375px・ダークテーマ): 表示崩れ・横スクロールなし、トークン色でテーマ追従。
  - 外部リクエスト: `/`・`/app`・`/app?sample=tokugawa-ieyasu` すべて自オリジンのみ(外部0件)。
  - ランディング表示時にAppチャンク(family-chart)は読み込まれない(0件)。`/app` では読み込まれる。
  - `/app?sample=tokugawa-ieyasu` でサンプルがエディタに描画され自動保存が動作することを目視確認。
