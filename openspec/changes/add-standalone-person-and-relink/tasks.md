## 1. ドメイン: 循環ガードの基盤(D7)

- [x] 1.1 `src/domain/commands.ts` に、指定人物が子として属する**全ての**Familyをたどって祖先集合を返すヘルパー(`collectAncestors(doc, personId)`)を追加する(完了条件: 実親・養親の双方を持つ人物で、両系統の祖先が漏れなく返り、循環を含むドキュメントでも無限ループしないことを検証する単体テストが通る)
- [x] 1.2 関係リンクの成立可否を判定する述語(自分自身でないこと、相手が自分の祖先・子孫でないこと)を追加する(完了条件: 自己参照・直系祖先・養親経由の祖先の3ケースで拒否され、いとこ婚に相当する構成では許可されることを検証する単体テストが通る)

## 2. ドメイン: 既存人物同士のリンク(D2)

- [x] 2.1 `linkSpouse(doc, personId, spouseId)` を追加する。既存の2人を配偶者とする新しいFamilyを作り、既存Familyへは合流させない(完了条件: どのFamilyにも属さない2人をリンクすると配偶者2件のFamilyが1件増え、既存のFamilyが変化しない単体テストが通る)
- [x] 2.2 `linkSpouse` に、同一人物同士・既に配偶者である相手・1.2 の循環条件に反する相手を拒否するガードを追加する(完了条件: 3ケースすべてで `throw` し、ドキュメントが変化しない単体テストが通る)
- [x] 2.3 `linkChild(doc, parentId, childId, options)` を追加する。`addChild` と同じ規則(相方指定があればその家族、なければ配偶者1件の家族、該当なしなら新設)で既存人物を子として帰属させる(完了条件: 相方あり・相方なし・該当家族なしの3経路で期待どおりのFamilyに帰属し、対象人物の氏名・生没日が変化しない単体テストが通る)
- [x] 2.4 `linkChild` に、既に同じ家族の子である場合は変更なしで返す挙動と、当該家族の配偶者・循環条件に反する人物を拒否するガードを追加する(完了条件: 再帰属でドキュメントが変化せず、配偶者本人と祖先が `throw` することを検証する単体テストが通る)
- [x] 2.5 `linkChild` で、既に他の家族に子として属している人物も帰属させられることを確認する(完了条件: 実親の家族に属する人物を養親の家族へ帰属させ、続柄を「養子」に変更すると双方の帰属が区別して取得できる単体テストが通る)
- [x] 2.6 `linkParent(doc, childId, parentId)` を追加する。`addParent` と同じ規則(配偶者1件の親家族があれば合流、なければ新設)で既存人物を親にする(完了条件: 合流・新設の両経路で期待どおりの構成になり、循環条件に反する場合は `throw` する単体テストが通る)
- [x] 2.7 既存の `addSpouse` / `addChild` / `addParent` / `addSpouseLink` / `addChildLink` の挙動が変わっていないことをテストで固定する(完了条件: 既存の `commands.test.ts` が変更なしで全件通る)

## 3. ドメイン: 関係リンクの解除(D3, D4)

- [x] 3.1 `unlinkChild(doc, familyId, childId)` を追加する。子リンクのみを外し、人物レコードは削除しない。結果が `isVacantFamily` に該当する家族は削除する(完了条件: 配偶者2件の家族では家族が存続し、配偶者1件の家族では家族ごと消え、いずれも人物が残る単体テストが通る)
- [x] 3.2 `unlinkSpouse(doc, familyId, personId)` を追加する。配偶者参照のみを外し、人物レコードは削除しない。結果が `isVacantFamily` に該当する家族は削除する(完了条件: 子ありの家族ではひとり親として存続し子の帰属とイベントが維持され、子なしの家族では家族ごと消える単体テストが両方通る)
- [x] 3.3 `computeUnlinkImpact(doc, familyId, target)` を追加し、削除される家族の有無・その婚姻/離婚イベント件数・子の帰属件数、および対象人物が解除後にどのFamilyにも属さなくなるかを返す(完了条件: 婚姻日ありの子なし夫婦からの離脱でイベント件数1・家族削除ありが返る単体テストが通る)
- [x] 3.4 `computeUnlinkImpact` と `unlinkChild` / `unlinkSpouse` が同一の判定関数(`isVacantFamily`)を経由することを構造として担保する(完了条件: 任意のドキュメントに対し、予告した家族削除の有無と実行後の家族数の差が一致することを検証する単体テストが通る)

## 4. 描画: 図に現れていない人物の算出(D5)

- [x] 4.1 `src/rendering/to-family-chart-data.ts` の `computeHiddenCounts` を、可視人物から到達できる非表示人物の集計に加えて、**どこからも到達できない非表示人物のID一覧**も返すよう拡張する(完了条件: 関係を持たない人物・切り離された夫婦クラスタ・折りたたみで隠れた傍系親族が混在するドキュメントで、それぞれが到達可能側/不可能側へ正しく振り分けられる単体テストが通る)
- [x] 4.2 同一人物がバッジ側と一覧側に同時に現れないことを確認する(完了条件: バッジに計上された人物IDと一覧のIDに重複がないことを検証する単体テストが通る)
- [x] 4.3 全体表示モードでは一覧が空になることを確認する(完了条件: 全体表示用データで描画した場合に一覧が空配列になる単体テストが通る)
- [x] 4.4 `src/rendering/FamilyTreeCanvas.tsx` で、既に算出している `visibleIds` を使って拡張後の戻り値を受け取り、一覧の対象を親コンポーネントへ渡せるようにする(完了条件: 人物の追加・関係の解除の直後に一覧の内容が更新されることを検証するテストが通る)

## 5. UI: 図に現れていない人物のトレイ(D5, D6)

- [x] 5.1 `src/components/UnconnectedTray.tsx` を新規作成し、対象人物を氏名のチップとして横並びに描画する(完了条件: 対象が3人のとき3件のチップが描画され、氏名で識別できるコンポーネントテストが通る)
- [x] 5.2 対象が0人のときはトレイの領域ごと描画しない(完了条件: 全員が図上にいる家系図でトレイのDOMが存在しないコンポーネントテストが通る)
- [x] 5.3 チップのクリックで人物を選択し、通常の編集パネルが開くようにする(完了条件: チップを選ぶと編集パネルにその人物の氏名が表示されるコンポーネントテストが通る)
- [ ] 5.4 `src/components/UnconnectedTray.css` を追加し、キャンバス下部に配置する。左下の全体表示トグルと重ならないようにする(完了条件: 実機(`npm run dev`)で、トグルとトレイが重ならず、対象が多い場合も横スクロールで破綻しないことを確認する)
- [x] 5.5 `src/App.tsx` / `FamilyTreeCanvas.tsx` へトレイを組み込む(完了条件: 人物を関係なしで追加するとトレイに現れ、関係をリンクするとトレイから消えることを検証するテストが通る)

## 6. UI: 関係を指定しない人物の追加

- [ ] 6.1 人物が1人以上ある状態で、関係を指定せずに人物を追加する導線を設ける(置き場所は design の Open Questions のとおり実装時に決める)(完了条件: 氏名を入力して追加するとどのFamilyにも属さない人物が1人増えるコンポーネントテストが通る)
- [ ] 6.2 追加した人物を直後に選択状態にする(完了条件: 追加操作の直後に編集パネルがその人物を表示しているコンポーネントテストが通る)
- [ ] 6.3 追加がundoで取り消せることを確認する(完了条件: 追加直後のundoで人物が消えるテストが通る)

## 7. UI: 既存の人物を関係先に選ぶ

- [ ] 7.1 `src/components/PersonPanel.tsx` の「配偶者を追加」「子を追加」「親を追加」の各フォームに、新規作成と既存人物選択を切り替える経路を設ける(完了条件: 3つの関係すべてで既存人物の候補一覧が開けるコンポーネントテストが通る)
- [ ] 7.2 候補一覧から、選択中の人物自身・既にその関係が成立している人物・循環条件に反する人物を除外する(完了条件: 既に配偶者である人物が配偶者候補に出ず、祖先が子の候補に出ないコンポーネントテストが通る)
- [ ] 7.3 候補の選択で `linkSpouse` / `linkChild` / `linkParent` を `apply` 経由で即座に実行する(完了条件: 選択と同時に図の描画データへ関係が反映され、`PersonEditForm` の離脱確認ダイアログが発生しないコンポーネントテストが通る)
- [ ] 7.4 「配偶者を追加 → 既存の人物から選ぶ」と、`FamilyEventEditor` の「配偶者に既存の人物を設定」が区別できる文言になっていることを確認する(完了条件: 前者が新しい婚姻を作る操作、後者が既存の家族を埋める操作であると読み取れることを実機で確認する)
- [ ] 7.5 リンクがundoで取り消せることを確認する(完了条件: リンク直後のundoで関係が消え、対象人物のデータは残るテストが通る)
- [ ] 7.6 `src/components/PersonPanel.css` に候補選択欄のスタイルを追加する(完了条件: 実機で関係追加フォームの視線の流れが破綻していないことを確認する)

## 8. UI: 関係リンクの解除

- [ ] 8.1 `src/components/PedigreeEditor.tsx` の各行に「この親子関係を解除」を追加する(完了条件: 子として属する家族の数だけ解除操作が描画されるコンポーネントテストが通る)
- [ ] 8.2 `src/components/FamilyEventEditor.tsx` の各家族に「この家族から自分を外す」を追加し、既存の「この婚姻を削除」と文言で区別する(完了条件: 両方の操作が並存し、それぞれ別の確認ダイアログを開くコンポーネントテストが通る)
- [ ] 8.3 8.1 / 8.2 の確認ダイアログを実装する(`confirm-dialog.css` と `DeletePersonControl` のパターンを再利用)。`computeUnlinkImpact` の結果から、家族ごと削除される場合はその旨と失われるイベント件数・子の帰属件数を、人物は削除されない旨と、undoで復元できる旨を提示する(完了条件: 婚姻日あり・子なしの家族からの離脱で、件数を含む確認文が表示されるコンポーネントテストが通る)
- [ ] 8.4 解除後に対象人物がどのFamilyにも属さなくなる場合、図から外れてトレイへ移る旨を確認ダイアログに追加する(完了条件: 1つの家族にのみ属する人物の解除でその文言が表示され、複数の関係を持つ人物では表示されないコンポーネントテストが両方通る)
- [ ] 8.5 承認時に `unlinkChild` / `unlinkSpouse` を `apply` 経由で実行し、キャンセルではドキュメントが変化しないようにする(完了条件: 承認・キャンセルの両方を検証するコンポーネントテストが通る)
- [ ] 8.6 解除がundoで取り消せることを確認する(完了条件: 解除直後のundoで関係・削除された家族・イベント・子の帰属が復元されるテストが通る)
- [ ] 8.7 `src/components/PedigreeEditor.css` / `FamilyEventEditor.css` に解除操作のスタイルを追加する(完了条件: 実機で続柄の `<select>` や日付欄と視覚的に競合せず、危険操作として区別できることを確認する)

## 9. 通し検証とドキュメント

- [ ] 9.1 「役割の取り違えの修正」を通しで検証する(完了条件: 子として登録し生年月日とメモを入力した人物について、親子関係の解除 → 配偶者としてのリンクを行い、図上で婚姻線が引かれ入力済みデータが保持されていることを検証するテストが通る)
- [ ] 9.2 「先に登録して後から繋ぐ」を通しで検証する(完了条件: 関係なしで2人を追加 → 互いを配偶者にリンク → 本体の人物へ子としてリンク、の順で操作し、各段階でトレイの内容が期待どおり変化することを検証するテストが通る)
- [ ] 9.3 関係を持たない人物を含むドキュメントのGEDCOM/JSONラウンドトリップを検証する(完了条件: エクスポート→インポートで関係を持たない人物が失われず、`FAMS`/`FAMC` を持たないINDIとして出力されるテストが通る)
- [ ] 9.4 `npm run lint` / `npm run test` / `npm run build` が通ることを確認する(完了条件: 3コマンドすべてが成功する)
- [ ] 9.5 `README.md` の「機能(無料版)」に、関係を指定しない人物の追加・既存人物のリンク・関係の解除・図に現れていない人物のトレイを反映する(完了条件: 既存の「配偶者の後からの紐づけ」の記述と重複・矛盾がないことを読み合わせて確認する)
