# Tasks: add-gedcom-json-import-export

## 1. 内部データモデル(src/domain)

- [ ] 1.1 zod を依存に追加し、日付型(原文・暦種別・元号・年月日・修飾子)と氏名型(表記・読み仮名・ローマ字・別表記)の型定義+zodスキーマを実装する
  - 完了条件: 「明治十年頃」相当の構造化値と旧字体表記を保持でき、暗黙の文字正規化が行われないことをユニットテストで確認
- [ ] 1.2 Person / Family の型定義+zodスキーマ+ID生成(UUID)を実装する(関係種別、続柄種別〈実子・養子・継子・里子〉、複数家族への所属、全属性の未入力許容)
  - 完了条件: 再婚(2家族所属)・養子の二重所属(実親「実子」+養親「養子」)・片親家族・氏名のみの人物がバリデーションを通ることをユニットテストで確認
- [ ] 1.3 データモデル全体の参照整合性バリデーション(家族が参照する人物IDの存在検証)を実装する
  - 完了条件: 欠落IDを参照する家族が整合性エラーとして報告され、正常なモデルはエラーなしで通ることをユニットテストで確認

## 2. 和暦⇄西暦変換(src/lib/wareki)

- [ ] 2.1 元号テーブル(明治以降の全元号+文化〜慶応の江戸後期元号、開始・終了日付き)をデータモジュールとして実装する
  - 完了条件: 各元号の開始日・終了日がテーブルから引け、追加が1エントリの追記で済む構造であることをユニットテストで確認
- [ ] 2.2 和暦→西暦変換を実装する(元年・改元境界・範囲外元号の変換失敗を含む)
  - 完了条件: 「昭和64年1月7日」→1989-01-07、範囲外元号「應永3年」→明示的な変換失敗になることをユニットテストで確認
- [ ] 2.3 西暦→和暦変換を実装する(改元境界で正しい元号を選択)
  - 完了条件: 1989-01-07→「昭和64年」・1989-01-08→「平成元年」となることをユニットテストで確認
- [ ] 2.4 日付文字列パーサを実装する(漢数字・修飾子〈頃・以前・以後・推定〉・年のみ等の不完全日付を構造化し、構造化不能な原文は原文のみの日付として返す)
  - 完了条件: 「明治十年頃」が元号・年・修飾子に構造化され、「戌年生まれ」が原文のみの日付として保持されることをユニットテストで確認

## 3. GEDCOM構文層(src/lib/gedcom)

- [ ] 3.1 GEDCOM行パーサを実装する(level / xref / tag / value の解釈、CONT/CONC の結合、行番号の保持、GedcomNode ツリーの構築)
  - 完了条件: 代表的なGEDCOM断片がツリー化され、不正な行が行番号付きのエラー/警告になることをユニットテストで確認
- [ ] 3.2 GEDCOMシリアライザを実装する(ツリー→テキスト、改行を含む値のCONT分割、長い行のCONC分割)
  - 完了条件: 構文層単体で parse→serialize→parse のラウンドトリップが等価になることをユニットテストで確認

## 4. 文字コード処理(src/lib/gedcom)

- [ ] 4.1 エンコーディング判定+デコードを実装する(BOM検出→UTF-8厳格デコード→失敗時Shift_JISフォールバック、UTF-16対応、CHARタグ参照、ANSEL検出時は中断エラー、判定結果を返却)
  - 完了条件: UTF-8(BOM有無)・UTF-16・Shift_JISの日本語フィクスチャが文字化けなく読め、ANSELが中断エラーになることをユニットテストで確認

## 5. GEDCOM意味層(バージョン判定・マッピング)

- [ ] 5.1 ヘッダからのバージョン自動判定(7.0 / 5.5.1)と、GEDCOMとして解釈不能な場合の中断エラーを実装する
  - 完了条件: 7.0/5.5.1ヘッダが正しく判定され、ヘッダなしテキストが平易なメッセージの中断エラーになることをユニットテストで確認
- [ ] 5.2 GEDCOM 7.0インポートマッパを実装する(INDI/FAM→内部モデル、PEDI続柄、TRAN読み仮名、DATE+PHRASE、未知タグのサブツリー保全、警告収集)
  - 完了条件: 養子・再婚・読み仮名・和暦PHRASEを含む7.0フィクスチャが内部モデルへ変換され、未知タグが保全領域に残ることをユニットテストで確認
- [ ] 5.3 GEDCOM 5.5.1インポートマッパを実装する(_KANA/ROMN読み仮名、日付句、PEDI/ADOPによる続柄、未知タグ保全。実ファイル慣行を確認し_KANA/ROMNの主従を決定してdesignのOpen Questionを解消する)
  - 完了条件: 読み仮名タグ両系統を含む5.5.1フィクスチャが内部モデルへ変換されることをユニットテストで確認
- [ ] 5.4 GEDCOM 7.0エクスポートマッパを実装する(内部モデル→ツリー、ヘッダGEDC.VERS 7.0、PEDI・TRAN・PHRASE出力、同一バージョン保全タグの復元、他バージョン由来タグの警告列挙)
  - 完了条件: 養子・読み仮名・和暦原文を含むモデルが仕様どおりの7.0構造で出力されることをユニットテストで確認
- [ ] 5.5 GEDCOM 5.5.1互換エクスポートマッパを実装する(VERS 5.5.1+CHAR UTF-8、表現不能な情報〈事実婚等〉のNOTE保全+警告、5.5.1由来保全タグの復元)
  - 完了条件: 事実婚を含むモデルの出力にNOTE代替と警告が含まれ、ヘッダが5.5.1+UTF-8であることをユニットテストで確認
- [ ] 5.6 同一バージョンのラウンドトリップ保全テストを追加する(独自タグ `_MYTAG` を含む5.5.1ファイル→インポート→5.5.1再エクスポートで元サブツリーが復元される)
  - 完了条件: ラウンドトリップ後のファイルに `_MYTAG` サブツリーが元の構造で含まれることをテストで確認

## 6. ネイティブJSON入出力(src/lib/json)

- [ ] 6.1 ネイティブJSONスキーマ(schemaVersion: 1、エクスポート日時、人物・家族配列、メタデータ)とエクスポート直列化を実装する
  - 完了条件: 保全タグ・日付原文・別表記を含むモデルの出力JSONに全情報が含まれることをユニットテストで確認
- [ ] 6.2 JSONインポートを実装する(zodスキーマ検証、schemaVersion欠落・未知バージョンの中断、既知旧バージョン用マイグレーション枠組み〈初版はv1のみ〉、失敗時は既存データ不変)
  - 完了条件: 正常v1が復元され、フィールド欠落・型不正・schemaVersion 999 が平易なメッセージで中断されることをユニットテストで確認
- [ ] 6.3 JSONラウンドトリップの等価性テストを追加する(養子・再婚・事実婚・和暦原文・旧字体別表記・保全タグを含む複雑モデルで エクスポート→インポート→深い等価比較)
  - 完了条件: 復元モデルが元モデルと深い等価であることをテストで確認

## 7. ファイル入出力UI(src/features/import-export)

- [ ] 7.1 インポートUIを実装する(ファイル選択+ドラッグ&ドロップ、拡張子による形式判別〈.ged/.json〉、20MB上限の事前チェック、中断エラーの平易な日本語表示)
  - 完了条件: D&Dとファイル選択が同一処理に合流し、20MB超・不正ファイルで既存データを変えずエラー表示されることをコンポーネントテストで確認
- [ ] 7.2 インポート結果サマリUIを実装する(人物数・家族数・判定エンコーディング・警告一覧〈行番号・対象・内容・処理〉の表示)
  - 完了条件: 警告付きフィクスチャのインポートでサマリと警告一覧が表示され、警告なしでは空の警告欄になることをコンポーネントテストで確認
- [ ] 7.3 エクスポートUIを実装する(GEDCOM 7.0/5.5.1互換/JSONの3形式選択+用途説明1行、個人情報を含む旨の注意書き、Blobダウンロード、エクスポート時警告の表示)
  - 完了条件: 各形式で正しい拡張子・内容のファイルがダウンロードされ、注意書きが表示されることをコンポーネントテストで確認
- [ ] 7.4 App.tsx に入出力UIを組み込み、インポートしたデータモデルをアプリ状態として保持して再エクスポートできるようにする
  - 完了条件: ブラウザ上でGEDCOMインポート→サマリ確認→3形式エクスポートが一連で操作できることを手動確認

## 8. 検証・仕上げ

- [ ] 8.1 相互運用フィクスチャテストを追加する(MyHeritage・Gramps・FamilySearchの出力形式を模した5.5.1/7.0サンプルのインポートが警告許容で成立する)
  - 完了条件: 各フィクスチャで人物・家族が取り込まれ、未対応部分が警告として列挙されることをテストで確認
- [ ] 8.2 クライアント完結の検証を行う(入出力処理コードにネットワークAPI呼び出しが存在しないことのレビュー+ブラウザNetworkタブで自ホスト静的アセット以外の通信がないことの確認)
  - 完了条件: インポート/エクスポート操作中に外部への通信が1件も発生しない
- [ ] 8.3 README.md を更新する(対応フォーマット〈GEDCOM 7.0 / 5.5.1互換 / JSON〉、文字コード対応、制限事項〈ANSEL非対応・サイズ上限・バージョンまたぎ保全の限界〉、クライアント完結の明記)
  - 完了条件: README の記載だけで対応フォーマットと制限事項が把握できる
- [ ] 8.4 品質ゲートを全通過させ、develop へのPRを作成して dev 環境で動作確認する
  - 完了条件: lint / typecheck / test / build が全て成功し、dev 環境URLでインポート→エクスポートの一連操作が動作する
